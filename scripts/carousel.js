const flky1 = new Flickity(".carousel", {
  // options
  autoPlay: true,
  wrapAround: true,
});

const flky2 = new Flickity(".carousel-notable", {
  // options
  cellAlign: "center",
  autoPlay: true,
  wrapAround: true,
});
